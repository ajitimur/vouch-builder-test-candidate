# Copy to .env and fill in. The live LLM extraction path uses this key; without it
# the service falls back to the committed fixture (fixtures/nightlog-claims.json).
ANTHROPIC_API_KEY=
# Optional: override the HTTP port (default 3000)
# PORT=3000
